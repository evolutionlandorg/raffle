{
  "scripts": {
    "build": "tronbox compile",
    "deploy:shasta": "source .env && tronbox migrate --network shasta --reset",
    "event1": "source .env && node scripts/raffle_event_1.js"
  },
  "dependencies": {
    "tronweb": "^3.2.6"
  }
}
